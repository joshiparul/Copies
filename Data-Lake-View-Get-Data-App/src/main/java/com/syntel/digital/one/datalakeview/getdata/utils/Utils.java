package com.syntel.digital.one.datalakeview.getdata.utils;

import java.sql.Connection;
import java.sql.DriverManager;

import com.syntel.digital.one.datalakeview.getdata.constants.Constants;

public class Utils {
	/*public static Connection getMysqlConnection() {
		Connection con = null;
		try {
			Class.forName(Constants.mysqlJDBCDriver);
			con = DriverManager.getConnection(Constants.mysqlJDBCHost, Constants.mysqlHost, Constants.mysqlPassword);
			// here sonoo is database name, root is username and password
		} catch (Exception e) {
			System.out.println(e);
		}
		return con;
	}*/

}
